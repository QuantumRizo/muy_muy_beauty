{
  "name": "muymuy-beauty-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "web": "npm run dev --workspace=apps/web",
    "docs:build": "node docs/build.js"
  },
  "devDependencies": {
    "md-to-pdf": "^5.2.5"
  }
}
